services:
  - type: web
    name: drivesafe-app
    env: python
    buildCommand: ./build.sh
    startCommand: cd drivesafe && gunicorn web_app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.7
      - key: PORT
        value: 5001
    plan: free
